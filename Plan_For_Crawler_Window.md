给这个爬虫项目做一个前端ui，只做前端部分。如下内容如果出现未实现后端部分，不允许假实现。请暂停并告知我。

业务：
1. 直接调用presenterTask里的这一个函数。
具体业务间的区别，只体现在传入的参数
主要开发应该发生在cppGUI目录（chatwindow和launcherwindow）下。业务包括触发：翻页、筛选、搜索、排序、爬取（见第2步）、查看具体信息（见第3步）。

- ui最上面是搜索框，搜索框右边是一个搜索键，和一个“爬取”键
搜索框下面是是一个列表；
- 列表：展示字段：jobId，工作名称，招聘类型名称，城市名称，薪资（拼接字符串“最低薪资-最高薪资”；若最低为0，则该字段展示为“面议”），来源，前3个tag。
- 筛选功能：表头可以对字段：薪金，Tag，城市，招聘类型，来源等进行筛选（类似excel表格的筛选，有下拉框里面有勾选，勾选后下面的表格里只显示勾选的）；
- 排序功能：表头可以进行任意单字段排序。

2. 爬取。直接调用crawlerTask里的这一个函数。牵动程序开展爬虫工作。
点击：“爬取”按钮后，打开设置弹窗。弹窗主体位置可设置字段：1）每个来源爬取的页数（spinBox）；2）需要爬取的来源（下拉框）。
（注意：暂停、爬取配置、来源列表constant尚未实现。先提供UI即可））

3. 查看。单条工作有一个小按钮，点击后弹出一个窗口，展示具体信息。

具体信息可以见constants/db_types.h的JobInfoPrint结构体。

ASSERT　Failed

Widget must be created in GUI thread.